#include <stdio.h>
//#define fact(x) ((x==1)?1:x*fact(x-1))
int fact(int x);
int main(){
  int n;
  scanf("%d",&n)
  printf("%d",fact(n));
}
int fact(int x){
  if(x==1){
  return 1;}
  else{
   return x*fact(x-1);
  }
}
