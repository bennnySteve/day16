#include <stdio.h>
#define mul(a,b) (a*b)
int main()
{
  int r,n,i,even=0,odd=0;
  scanf("%d%d",&r,&n);
  for(i=1;i<=r;i++){
    if(i%n==0){
      printf("%d\n",i);
      if(i%2==0)
      even+=i;
      else
      odd+=i;
    }
    
  }
  printf("%d %d",even,odd);
}
